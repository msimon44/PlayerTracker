server_tokens off;

server {
    listen 80;
    listen [::]:80;
    server_name ${SERVER_NAME};

    root /usr/share/nginx/html;
    index index.html;

    # Pour les requêtes vers ce serveur
    location / {
        try_files $uri $uri/ /index.html;
    }
}